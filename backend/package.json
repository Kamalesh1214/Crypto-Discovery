{
  "name": "cryptography-discovery-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-deps": "python -m pip install -r requirements.txt",
    "dev": "python -m uvicorn app.main:app --reload",
    "start": "python -m uvicorn app.main:app"
  }
}